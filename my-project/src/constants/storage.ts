/**
 * LocalStorage keys used throughout the application.
 * Centralized here to avoid duplicating hardcoded strings.
 */
export const STORAGE_KEYS = {
  // Authentication context - stores user and token
  AUTH: 'rtb-skills-gap-auth',
};
